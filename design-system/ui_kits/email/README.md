# Email — UI kit

The transactional email sent to applicants with their result and login credentials.

- `index.html` — entry point.
- `SKRCEmail.jsx` — 600px centered frame: gradient header band, body copy, an embedded
  `CredentialSlip`, a primary CTA, and a surface-2 footer band with mono fine print.
